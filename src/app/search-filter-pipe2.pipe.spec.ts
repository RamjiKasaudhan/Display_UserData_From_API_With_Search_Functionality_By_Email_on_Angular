import { SearchFilterPipe2Pipe } from './search-filter-pipe2.pipe';

describe('SearchFilterPipe2Pipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterPipe2Pipe();
    expect(pipe).toBeTruthy();
  });
});
